{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run my-career",
            "type": "debugpy",
            "request": "launch",
            "module": "my_career.main",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}
