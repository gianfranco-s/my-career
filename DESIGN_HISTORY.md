1. Create simple script to read JSON resume and cover letter.
2. Create PDF exporter.
3. PDF exporter is actually an adapter, so a refactor is done.
4. Create upstream tailor for JD.
5. Create prompt handler.
6. Create HTTP API handling, as adapter, and use_cases/
7. In previous step, ports/ were not needed. Now add them for completeness.
8. Add authentication+authorization via JWT. JWT is generated by companion auth service.
9. Add /health endpoint
10. Use pydantic-settings for configs
